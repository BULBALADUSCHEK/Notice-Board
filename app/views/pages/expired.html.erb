<h1 class="text-center mt-4">Welcome to the page with expired advertisements</h1> 
 <% if @articles.try(:any?) %>

  <div class="container">
    <% @articles.each do |article| %>
    <% if article.state == 'Expired' %>
    <div class="row justify-content-md-center">
      <div class="col-8 mt-4">
        <div class="card text-center shadow mb-5 bg-white rounded">
          <div class="card-header font-italic">
          by User_1
          <p>State: <%= article.state %></p>
          </div>
            <div class="card-body">
              <h5 class="card-title"><%= link_to article.title, article_path(article), class: "text-success" %></h5>        
                <p class="card-text"><%= article.description %></p>                
                <%= link_to "Comment", article_path(article), class: "btn btn-outline-info" %>                     
                <%= link_to "Edit", edit_article_path(article), class: "btn btn-outline-info" %>
 
                <div class="card-footer text-muted">
                  <small>Expired at: <%= article.expiration_date %>, 
                  edited <%= time_ago_in_words(article.updated_at) %> ago</small>       
                </div>
        </div>    
      </div>
    </div>
    <% end %>
    <% end %>
  </div>
  <% end %>
  
